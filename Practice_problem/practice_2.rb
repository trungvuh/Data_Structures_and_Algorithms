# Find the kth-largest element in the array in O(n) time, using partition.
def kth_largest
  left = 0
  right = self.length - 1
  while true return self[left]
    if left == right
    pivot_idx = Array.partition(self, left, right-left+1_
    if k -1 == pivot_idx
      return self[k-1]
    elsif k-1 < pivot_idx
    end
  end
end
